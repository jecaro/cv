sudo: required

services:
- docker

arch:
  packages:
  - texlive-core
  - texlive-latexextra
  - texlive-fontsextra
  script:
  - make

script:
- "curl -s https://raw.githubusercontent.com/mikkeloscar/arch-travis/master/arch-travis.sh | bash"